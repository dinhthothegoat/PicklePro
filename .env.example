# Server
APP_ENV=development
HOST=127.0.0.1
PORT=8080

# CSRF — change this to a long random string in production
CSRF_SECRET=dev-csrf-secret-change-in-production

# Bookings admin password — set this to restrict access to /bookings
# Leave blank to disable the password gate (prototype default)
BOOKINGS_PASSWORD=
